#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
const double pi = 3.14159265358979323846;
const long long mod = 1e9 + 7;
#define oneMillion 1000000
#define go_mahedi_go ios::sync_with_stdio(false), cin.tie(nullptr)
 
 
void mahedi_solve_this_prb()
{
    int n;
    cin>>n;
    int mini_lcm=INT_MAX;
    int a=0;
 
    for(int i=2; i*i<=n; i++)
    {
        if(n%i==0)
        {
            int d1=i;
            int d2=n/i;
 
            int cur_lcm=lcm(d1,n-i);
 
            if(cur_lcm<mini_lcm)
            {
                mini_lcm=cur_lcm;
                a=d1;
            }
 
 
            cur_lcm=lcm(d2,n-d2);
 
            if(cur_lcm<mini_lcm)
            {
                mini_lcm=cur_lcm;
                a=d2;
            }
 
        }
    }
 
    //cout<<"a = "<<a<<"\n";
    if(a==0) cout<<1<<" "<<n-1;
    else  cout<<a<<" "<<n-a;
 
}
 
 
int main()
{
    //go_mahedi_go;
    int t;
    cin>>t;
    //t=1;
 
    while(t--)
    {
        mahedi_solve_this_prb();
        cout<<"\n\n\n";
 
    }
}
 
