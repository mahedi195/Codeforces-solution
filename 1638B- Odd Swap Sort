#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
#define mod 1e9+7;
#define go_mahedi_go ios::sync_with_stdio(false), cin.tie(nullptr);
 
mahedi_solve_this_prb()
{
    int n;
    cin>>n;
    vector<int>v(n);
    for(int i=0; i<n; i++) cin>>v[i];
 
    vector<int>odd,even;
    for(int i=0;i<n;i++)
    {
        if(v[i]%2==0) even.push_back(v[i]);
        else odd.push_back(v[i]);
    }
 
    if(is_sorted(even.begin(),even.end()) &&
        is_sorted(odd.begin(),odd.end()))
            cout<<"YES";
    else cout<<"NO";
}
 
int main()
{
    //go_mahedi_go;
 
    int t;
    cin >> t;
    //t=1;
    while (t--)
    {
        mahedi_solve_this_prb();
        cout<<"\n";
        cout<<"\n";
    }
}
