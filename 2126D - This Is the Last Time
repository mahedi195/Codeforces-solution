#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
#define mod 1e9+7;
#define go_mahedi_go ios::sync_with_stdio(false), cin.tie(nullptr);
const double pi = 3.14159265358979323846;
//cout<<fixed<<setprecision(6)<<ans;
#define oneMillion 1000000

void mahedi_solve_this_prb()
{
    int n,k;
    cin>>n>>k;
    vector<pair<int,pair<int,int>>>p(n);
    for(int i=0; i<n; i++)
        cin>>p[i].first>>p[i].second.first>>p[i].second.second;
 
    sort(p.begin(),p.end());
 
    for(int i=0; i<n; i++)
    {
        if(p[i].first>k) break;
        if(p[i].first<=k && k<=p[i].second.first)
            k=max(k,p[i].second.second);
    }
    cout<<k;
 
 
}
 
 
 
int main()
{
    // go_mahedi_go;
 
    int t;
    cin >> t;
    // t=1;
    while (t--)
    {
        //solve();
        mahedi_solve_this_prb();
        cout<<"\n";
        cout<<"\n";
    }
}
