#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
#define mod 1e9+7;
#define go_mahedi_go ios::sync_with_stdio(false), cin.tie(nullptr);

void mahedi_solve_this_prb()
{
    int n,l,r;
    cin>>n>>l>>r;
    vector<int>v(n),v1(n);
    for(int i=0; i<n; i++) cin>>v[i];
    v1=v;
    l--;
    //r;
 
    sort(v.begin(),v.begin()+r);
    sort(v1.begin()+l,v1.end());
    // for(auto it:v) cout<<it<<" ";
    // cout<<"\n---\n";
    // for(auto it:v1) cout<<it<<" ";
    //cout<<"\n";
    ll s1=0,s2=0;
    //r--;
    for(int i=0; i<r-l; i++) s1+=v[i];
    for(int i=l; i<r; i++) s2+=v1[i];
    //cout<<s1<<" "<<s2<<"\n";
    cout<<min(s1,s2);
 
 
}
 
int main()
{
    //go_mahedi_go;
 
    int t;
    cin >> t;
    //t=1;
    while (t--)
    {
        mahedi_solve_this_prb();
        cout<<"\n";
        cout<<"\n";
    }
}
