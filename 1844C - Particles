#include <bits/stdc++.h>
using namespace std;
using  ll=long long;
const double pi = 3.14159265358979323846;
const long long mod = 1e9 + 7;
#define oneMillion 1e6
#define go_mahedi_go ios::sync_with_stdio(false), cin.tie(nullptr)
 
 
void mahedi_solve_this_prb()
{
    int n;
    cin>>n;
    vector<int>v(n);
    for(int i=0; i<n; i++)
        cin>>v[i];
 
    ll odd_sum=0;
    //int odd_max=INT_MIN;
    for(int i=0; i<n; i+=2)
    {
        if(v[i]>0)
        {
            odd_sum+=v[i];
            //odd_max=max(odd_max,v[i]);
        }
    }
 
    ll even_sum=0;
    //int even_max=INT_MIN;
    for(int i=1; i<n; i+=2)
    {
        if(v[i]>0)
        {
            even_sum+=v[i];
            //even_max=max(even_max,v[i]);
        }
    }
 
    if(odd_sum==0 && even_sum==0)
    {
        cout<<*max_element(v.begin(),v.end());
        return;
    }
    else cout<<max(odd_sum,even_sum);
 
}
 
 
 
 
int main()
{
    //go_mahedi_go;
    int t;
    cin>>t;
    // t=1;
 
 
    while(t--)
    {
        mahedi_solve_this_prb();
        cout<<"\n\n\n";
 
    }
}
 
